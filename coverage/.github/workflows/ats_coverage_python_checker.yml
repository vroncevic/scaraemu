name: ats_coverage_python_checker

on:
  push:
    branches: [ main ]
    paths:
      - 'ats_coverage.py'
      - 'ats_updater.py'
      - 'setup.py'
  pull_request:
    branches: [ main ]
    paths:
      - 'ats_coverage.py'
      - 'ats_updater.py'
      - 'setup.py'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Check module length and line limits
        run: |
          python3 gates/gates/limits_checker.py .
